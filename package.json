{
  "name": "lapor-pak-manang",
  "private": true,
  "version": "0.1.0",
  "description": "LAPOR PAK MANANG – government-grade civic reporting platform (monorepo: web + api).",
  "workspaces": [
    "web",
    "api",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace web",
    "dev:api": "npm run start:dev --workspace api",
    "dev": "concurrently \"npm run dev:web\" \"npm run dev:api\"",
    "build": "npm run build --workspace web",
    "build:all": "npm run build --workspace web && npm run build --workspace api",
    "lint": "npm run lint --workspace web && npm run lint --workspace api",
    "test": "npm test --workspace api",
    "format": "npm run format --workspace api || echo \"no format script in web yet\""
  },
  "dependencies": {
    "next": "16.1.6"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}

